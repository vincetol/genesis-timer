import styles from "./progressBar.module.css";

export default function ProgressBar({}) {
  // calc Progress in Hook
  // currenTime -> timerlength / breaktime (if 4 cycles = 2 * breaktime)
  return <div>Progressbar</div>;
}
